(rule (target Makefile.ml) (deps .././Makefile.eliom)
  (action
    (with-stdout-to %{target}
      (chdir .. (run tools/eliom_ppx_client.exe --as-pp -server-cmo %{cmo:.././Makefile} --impl %{deps})))))
(rule (target blibli.ml) (deps .././blibli.eliom)
  (action
    (with-stdout-to %{target}
      (chdir .. (run tools/eliom_ppx_client.exe --as-pp -server-cmo %{cmo:.././blibli} --impl %{deps})))))
(rule (target game.ml) (deps ../services/game.eliom)
  (action
    (with-stdout-to %{target}
      (chdir .. (run tools/eliom_ppx_client.exe --as-pp -server-cmo %{cmo:../services/game} --impl %{deps})))))
(rule (target game_state.ml) (deps ../services/game_state.eliom)
  (action
    (with-stdout-to %{target}
      (chdir .. (run tools/eliom_ppx_client.exe --as-pp -server-cmo %{cmo:../services/game_state} --impl %{deps})))))
(rule (target main.ml) (deps ../services/main.eliom)
  (action
    (with-stdout-to %{target}
      (chdir .. (run tools/eliom_ppx_client.exe --as-pp -server-cmo %{cmo:../services/main} --impl %{deps})))))
(rule (target newgame.ml) (deps ../services/newgame.eliom)
  (action
    (with-stdout-to %{target}
      (chdir .. (run tools/eliom_ppx_client.exe --as-pp -server-cmo %{cmo:../services/newgame} --impl %{deps})))))
(rule (target shared.ml) (deps ../services/shared.eliom)
  (action
    (with-stdout-to %{target}
      (chdir .. (run tools/eliom_ppx_client.exe --as-pp -server-cmo %{cmo:../services/shared} --impl %{deps})))))
